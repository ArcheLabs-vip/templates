---
import { site, type NavigationItem } from "../../data/site";
import Container from "../ui/Container.astro";

interface Props {
  navigation?: NavigationItem[];
}

const { navigation = site.navigation } = Astro.props;
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <Container class="site-footer__inner">
    <div>
      <a class="site-footer__brand" href="/">{site.name}</a>
      <p>{site.description}</p>

      <address class="site-footer__contact">
        <p><strong>Telefone:</strong> <a href={site.contact.phoneHref}>{site.contact.phone}</a></p>
        <p><strong>Endereço:</strong> {site.location.displayAddress}</p>
      </address>
    </div>

    <nav aria-label="Navegação do rodapé">
      <ul>
        {
          navigation.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))
        }
      </ul>
    </nav>

    <div class="site-footer__bottom">
      <p class="site-footer__rt">
        Responsável Técnico: {site.responsibleDoctor.name} — {site.responsibleDoctor.crm}
      </p>
      <p class="site-footer__legal">© {year} {site.name}. Todos os direitos reservados.</p>
    </div>
  </Container>
</footer>

<style>
  .site-footer {
    --color-primary: var(--color-dark-text);
    --color-text: var(--color-dark-text);
    --color-text-muted: var(--color-dark-muted);
    --color-border: var(--color-dark-border);
    border-top: 1px solid var(--color-border);
    background: var(--color-dark);
    color: var(--color-dark-text);
  }

  .site-footer__inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-7) var(--space-6);
    padding-block: var(--space-8);
  }

  .site-footer__brand {
    color: var(--color-primary);
    font-family: var(--font-heading);
    font-size: 1.2rem;
    font-weight: 800;
    letter-spacing: -0.01em;
    text-decoration: none;
  }

  .site-footer p {
    max-width: 34rem;
    margin-block: var(--space-3) 0;
    color: var(--color-text-muted);
    font-weight: 400;
  }

  .site-footer__contact {
    font-style: normal;
    margin-top: var(--space-5);
  }

  .site-footer__contact p {
    margin-block: var(--space-1) 0;
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
  }

  .site-footer__contact a {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
  }

  .site-footer__contact a:hover {
    text-decoration: underline;
  }

  ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-4) var(--space-5);
    padding: 0;
    margin: 0;
    list-style: none;
  }

  nav a {
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
    font-weight: 550;
    text-decoration: none;
  }

  nav a:hover {
    color: var(--color-text);
  }

  .site-footer__bottom {
    grid-column: 1 / -1;
    padding-top: var(--space-5);
    border-top: 1px solid var(--color-border);
  }

  .site-footer__rt {
    color: var(--color-text-muted);
    font-size: var(--font-size-sm);
    font-weight: 500;
    margin: 0;
  }

  .site-footer__legal {
    font-size: var(--font-size-sm);
    margin-block: var(--space-2) 0;
  }

  @media (max-width: 47.99rem) {
    .site-footer__inner {
      grid-template-columns: 1fr;
    }

    ul {
      justify-content: flex-start;
    }

    .site-footer__bottom {
      grid-column: auto;
    }
  }
</style>
